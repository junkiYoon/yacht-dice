# Backend URL — leave empty to auto-detect from the browser's hostname (recommended).
# Set only for non-standard setups (e.g., backend on a different machine/port).
VITE_API_URL=

# Override the backend port when auto-detecting (default: 3000)
VITE_API_PORT=3000
