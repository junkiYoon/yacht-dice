# Port the backend server listens on
PORT=3000

# Allowed CORS origin. Use '*' (default) to allow all origins for LAN play.
# Restrict to a specific origin in production, e.g. https://yourdomain.com
CORS_ORIGIN=*
